export type TimeData = {
  date: string;
  startTime: string;
  closeTime: string;
};

export type InputData = {
  name: string;
  categoryId: string;
  intro: string;
  pickedDate: Date;
  durationTime: number;
  startTime: string;
  maxPeopleNumber: number;
  price: number;
};
